{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "TerminalInterpolationRequest",
  "description": "Request body for retrieving stored interpolation results for a customer from the /terminal/interpolation endpoint.",
  "type": "object",
  "properties": {
    "customer_id": {
      "type": "string",
      "description": "The identifier for the customer.",
      "examples": ["demo-customer"]
    }
  },
  "required": ["customer_id"]
}
